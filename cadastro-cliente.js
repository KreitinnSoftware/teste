function register(event){event.preventDefault();const db=loadDb(),form=event.target,email=form.email.value.trim().toLowerCase();if(db.users.some(user=>user.email===email)){alert('E-mail já cadastrado');return}db.nid=db.nid||{u:1,d:1,o:1};db.users.push({id:db.nid.u++,nome:form.nome.value.trim(),email,senha:form.senha.value,perfil:'cliente',tel:form.tel.value});saveDb(db);location.href='index.html'}
